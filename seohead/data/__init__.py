"""Reference data that is intentionally stored in the repository.

Audits must be reproducible: yesterday's report should produce the same result today.
The Schema.org vocabulary is therefore pinned to a dated local snapshot instead of being
downloaded on every run. Update it deliberately, recording the source URL, licence, and date.
"""
